export default function Card() {
    title="default",
    description="default",
    bgColor,
    borderColor

    return (
        <>
            <div className={styles.menuItem} style={{backgroundColor: bgColor, color=textColor, borderColor: border}}>

</div>
        </>
    )
}